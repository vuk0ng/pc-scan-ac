<UserControl x:Class="GModForensic.App.Views.ScanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:GModForensic.App.Converters">

  <UserControl.Resources>
    <conv:ToneToBrushConverter x:Key="Tone" />
    <conv:FractionToPercentConverter x:Key="Percent" />
    <conv:NotEmptyToVisibilityConverter x:Key="NotEmpty" />
  </UserControl.Resources>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0">
      <TextBlock Style="{StaticResource Eyebrow}" Text="ANALYSE DU SYSTÈME" />
      <TextBlock Style="{StaticResource H1}" Text="Scan en cours" />
    </StackPanel>

    <StackPanel Grid.Row="1" Margin="0,20,0,18">
      <ProgressBar Minimum="0" Maximum="100"
                   Value="{Binding OverallFraction, Mode=OneWay, Converter={StaticResource Percent}}" />

      <Grid Margin="0,12,0,0">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0">
          <TextBlock Style="{StaticResource Muted}" Text="Étape actuelle" />
          <TextBlock Text="{Binding CurrentStep}" TextWrapping="Wrap" Margin="0,2,0,0" />
        </StackPanel>

        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
          <StackPanel Margin="24,0,0,0">
            <TextBlock Style="{StaticResource Muted}" Text="Progression" />
            <TextBlock Style="{StaticResource H2}" Margin="0,2,0,0" Text="{Binding OverallPercentText}" />
          </StackPanel>
          <StackPanel Margin="24,0,0,0">
            <TextBlock Style="{StaticResource Muted}" Text="Éléments analysés" />
            <TextBlock Style="{StaticResource H2}" Margin="0,2,0,0" Text="{Binding ItemsExamined}" />
          </StackPanel>
          <StackPanel Margin="24,0,0,0">
            <TextBlock Style="{StaticResource Muted}" Text="Indicateurs" />
            <TextBlock Style="{StaticResource H2}" Margin="0,2,0,0" Text="{Binding ObservationsText}" />
          </StackPanel>
          <StackPanel Margin="24,0,0,0">
            <TextBlock Style="{StaticResource Muted}" Text="Temps écoulé" />
            <TextBlock Style="{StaticResource H2}" Margin="0,2,0,0" Text="{Binding ElapsedText}" />
          </StackPanel>
        </StackPanel>
      </Grid>
    </StackPanel>

    <Grid Grid.Row="2">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="14" />
        <ColumnDefinition Width="440" />
      </Grid.ColumnDefinitions>

      <Border Grid.Column="0" Style="{StaticResource Card}">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
          <ItemsControl ItemsSource="{Binding Modules}">
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <Grid Margin="0,5">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="22" />
                    <ColumnDefinition Width="250" />
                    <ColumnDefinition Width="70" />
                    <ColumnDefinition Width="*" />
                  </Grid.ColumnDefinitions>

                  <!-- Le symbole et le texte portent l'information ; la couleur ne fait que renforcer. -->
                  <TextBlock Grid.Column="0" Text="{Binding Symbol}"
                             Foreground="{Binding Tone, Converter={StaticResource Tone}}" />
                  <TextBlock Grid.Column="1" Text="{Binding DisplayName}" TextTrimming="CharacterEllipsis" />
                  <TextBlock Grid.Column="2" Style="{StaticResource Mono}" TextAlignment="Right"
                             Text="{Binding Fraction, StringFormat={}{0:P0}}" />
                  <TextBlock Grid.Column="3" Style="{StaticResource Muted}" Margin="14,0,0,0"
                             Text="{Binding Detail}" TextTrimming="CharacterEllipsis" />
                </Grid>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </ScrollViewer>
      </Border>

      <Border Grid.Column="2" Style="{StaticResource Card}">
        <DockPanel>
          <TextBlock DockPanel.Dock="Top" Style="{StaticResource H2}" Text="Journal d'exécution" />
          <ListBox ItemsSource="{Binding LogLines}" Background="Transparent" BorderThickness="0"
                   ScrollViewer.HorizontalScrollBarVisibility="Auto">
            <ListBox.ItemTemplate>
              <DataTemplate>
                <TextBlock Style="{StaticResource Mono}" Text="{Binding}" FontSize="11" />
              </DataTemplate>
            </ListBox.ItemTemplate>
          </ListBox>
        </DockPanel>
      </Border>
    </Grid>

    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
      <TextBlock Style="{StaticResource Muted}" VerticalAlignment="Center" Margin="0,0,16,0"
                 Text="Une annulation conserve les résultats déjà obtenus."
                 Visibility="{Binding IsRunning, Converter={StaticResource NotEmpty}}" />
      <Button Content="Annuler" Command="{Binding CancelCommand}" />
    </StackPanel>
  </Grid>
</UserControl>
